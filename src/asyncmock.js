// Simulacion de una peticion a un servidor que estamos desarrollando a nivel de la carpeta src
// Tiene base de datos y una funcion que retorna una promesa con una demora de 2 segundos para gemnerar retrasos en la red

/* const productos = [
  {
    nombre: "Simple",
    precio: 1500,
    id: "1",
    stock: 10,
    img: "../img/simple.png",
    descripcion: "Hamburguesa Simple",
    idCat: "2",
  },
  {
    nombre: "Doble",
    precio: 1750,
    id: "2",
    stock: 10,
    img: "../img/doble.png",
    descripcion: "Hamburguesa Doble",
    idCat: "2",
  },
  {
    nombre: "Triple",
    precio: 2000,
    id: "3",
    stock: 10,
    img: "../img/triple.png",
    descripcion: "Hamburguesa Triple",
    idCat: "2",
  },
  {
    nombre: "Big Combo",
    precio: 2500,
    id: "4",
    stock: 10,
    img: "../img/combo.png",
    descripcion: "Combo de hamburguesa con papas",
    idCat: "3",
  },
];

export const getProductos = () => {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve(productos);
    }, 100);
  });
};

// Creamos una funcion similar pero que nos retorne un solo item (para el detalle del producto)
export const getUnProducto = (id) => {
  return new Promise((resolve) => {
    setTimeout(() => {
      const producto = productos.find((prod) => prod.id === id);
      resolve(producto);
    }, 100);
  });
};

// Funcion que retorna por categoria (todos los prod de una determinada categoria)
export const getProductosPorCategoria = (idCategoria) => {
  return new Promise((resolve) => {
    setTimeout(() => {
      const productosCategoria = productos.filter(
        (prod) => prod.idCat === idCategoria
      );
      resolve(productosCategoria);
    }, 100);
  });
};
 */
